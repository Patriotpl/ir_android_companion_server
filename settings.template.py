# remove ".template" from the name of this file
# create "env" folder inside the app's main directory
# move this file to the "env" folder

# connection settings
CLIENT_ADDRESS = ("ip", 0)  # ip addresses should be of str type; replace 0 with correct port
SERVER_ADDRESS = ("ip", 0)  # ip addresses should be of str type; replace 0 with correct port

# persistent variables
LAPS_TO_CALC_AVG = 5